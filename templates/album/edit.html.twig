{% extends 'base.html.twig' %}

{% block title %}Album bearbeiten{% endblock %}

{% block body %}
    <div class="container card mt-5 p-3">
        <h1>Album bearbeiten</h1>

        {{ include('album/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="row mt-3">
            <div class="col-md-6">
                <a href="{{ path('app_album_index') }}" class="btn w-100 btn-primary"><i class="mdi mdi-chevron-left me-1"></i>Zurück</a>
            </div>
            <div class="col-md-6">
                {{ include('album/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}

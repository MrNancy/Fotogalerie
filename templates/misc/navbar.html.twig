<nav class="navbar navbar-expand-lg navbar-dark bg-transparent">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_index') }}">{{ pagename }}</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link {% if route in ['app_dashboard'] %}active{% endif %}" aria-current="page" href="{{ path('app_dashboard') }}">Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if route in ['app_logout'] %}active{% endif %}" aria-current="page" href="{{ path('app_logout') }}">Abmelden</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>